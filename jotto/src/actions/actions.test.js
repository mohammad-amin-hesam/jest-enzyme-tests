import moxios from "moxios";

describe("get secret word action creator", () => {
  beforeEach(() => {
    moxios.install();
  });
  afterEach(() => {
    moxios.uninstall();
  });

  it("add response word to state", () => {});
});

// 3 from part 7 finished
